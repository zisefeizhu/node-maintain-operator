
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: nodemaintains.batch.node.maintain
spec:
  group: batch.node.maintain
  names:
    kind: NodeMaintain
    listKind: NodeMaintainList
    plural: nodemaintains
    shortNames:
    - nm
    singular: nodemaintain
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.nodeCount
      name: NodeCount
      type: integer
    - jsonPath: .status.notReadyNodeCount
      name: NotReadyNodeCount
      type: integer
    name: v2alpha1
    schema:
      openAPIV3Schema:
        description: NodeMaintain is the Schema for the nodemaintains API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NodeMaintainSpec defines the desired state of NodeMaintain
            properties:
              handler:
                description: Handler 对应 Runtime class 的Handler
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Labels 标签
                type: object
              taints:
                description: Taints 污点
                items:
                  description: The node this Taint is attached to has the "effect"
                    on any pod that does not tolerate the Taint.
                  properties:
                    effect:
                      description: Required. The effect of the taint on pods that
                        do not tolerate the taint. Valid effects are NoSchedule, PreferNoSchedule
                        and NoExecute.
                      type: string
                    key:
                      description: Required. The taint key to be applied to a node.
                      type: string
                    timeAdded:
                      description: TimeAdded represents the time at which the taint
                        was added. It is only written for NoExecute taints.
                      format: date-time
                      type: string
                    value:
                      description: The taint value corresponding to the taint key.
                      type: string
                  required:
                  - effect
                  - key
                  type: object
                type: array
            type: object
          status:
            description: NodeMaintainStatus defines the observed state of NodeMaintain
            properties:
              allocatable:
                additionalProperties:
                  anyOf:
                  - type: integer
                  - type: string
                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                  x-kubernetes-int-or-string: true
                description: 允许被调度的容量
                type: object
              nodeCount:
                description: 节点的数量
                type: integer
              notReadyNodeCount:
                description: 未准备好节点计数
                type: integer
              status:
                description: status=200 说明正常，其他情况为异常情况
                type: string
            required:
            - notReadyNodeCount
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
